package tercerhombre

import tercerhombre.personaje.Personaje;

rule "Personaje añadido"
    when
        p : Personaje(  )
    then
        System.out.println( "Personaje añadido: " + p.getNombre() );
        
end

rule "Personaje muerto"
    when
        p : Personaje( estadoSalud == EstadoSalud.MUERTO )
    then
        System.out.println( "Personaje muerto: " + p.getNombre() );
end
